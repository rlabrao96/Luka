# Next Steps

_Last updated: 2026-04-27 (Plaid OAuth + category-edit auth + split unification + deploy fix)_

## Recently Shipped

- **Plaid OAuth + category authorization sweep + schema unification + deploy fix (2026-04-27)** — Long debugging session that converted a stack of latent bugs into shipped fixes. **(1) Plaid OAuth round-trip:** `LinkTokenCreateRequest` now passes `redirect_uri` (`PLAID_REDIRECT_URI` env var on Railway, `https://luka-lovat.vercel.app/plaid-oauth` allowlisted in the Plaid dashboard); `PlaidLinkButton` persists the link_token to `localStorage` before opening; new `frontend/app/plaid-oauth/page.tsx` re-instantiates `usePlaidLink({ token, receivedRedirectUri })` and resumes the flow. OAuth banks (Chase / BofA / Wells / Citi / Cap One / Amex …) used to dead-end on `cdn.plaid.com`'s "Continue to Luka" page; now they land back inside the app and complete `exchange-token` cleanly. Camila's American Express linked end-to-end with 235 transactions populated. **(2) Active-membership filter:** `Transaction.user_id == user_id` was crashing with `MultipleResultsFound` whenever a user had any closed historical household membership. `plaid/router.py` exchange and `bank_connect/accounts.py` resolution now filter on `HouseholdMember.left_at IS NULL`, matching the unique partial index. **(3) Initial Plaid sync retries:** `run_plaid_sync` retries up to 5× with backoff (3, 5, 8, 13, 21s) when `initial=True` and Plaid returns empty — the first `/transactions/sync` after `public_token` exchange can take 5–30s while Plaid finishes ingesting (especially OAuth banks). Subsequent syncs run once. **(4) Category auth + delete fixes:** `update_category` and `update_split_type` now authorize on active household membership, so partners can recategorize each other's shared rows. `get_category_usage` and `delete_category` switched to household-scoped counts on `Transaction.category` (the canonical column). New "Dejar sin categoría" radio in the delete modal sends `reclassify_to=null` and the backend explicitly nulls out matching rows — categories never leave orphan strings. **(5) Migration 041:** `transaction_splits.category` dropped. The schema carried the same logical value in two columns; only WhatsApp ever wrote to the split copy, so web-UI categorizations diverged silently and the count query (which read from the split column) returned 0 — the exact reason Camila's "Ropa" delete trashed the preference without reclassifying its 32 transactions. Migration backfills any orphan WhatsApp-only split values back to `transactions.category` before dropping. `whatsapp/handler.py:_save_split` no longer writes the split column. **(6) Default split helper:** new `ensure_default_split(db, txn, default_split_type='personal')` in `transactions/service.py`, called from every creation site (`plaid/sync.py`, `jobs/tasks.py`, `bank_connect/router.py`, `whatsapp/handler.py`) — joint accounts default to `shared`, everything else `personal`, transfers stay split-less. Eliminates rows with no `transaction_splits` entry, which was a quiet correctness gap. **(7) asyncpg date coercion:** `households/service.py` `get_settlement` and `get_category_breakdown` were binding raw strings (`f"{month}-01"`) for DATE params, returning 500s on every settlement / category-breakdown call. Coerced via `date.fromisoformat(...)`. **(8) Deploy fix:** Railway honours the Dockerfile `CMD` on the API service even when `railway.toml` specifies `releaseCommand` / `startCommand`. Migration 041 had to be applied by hand because the toml's chained `alembic upgrade head` was silently ignored. `alembic upgrade head` is now baked into the Dockerfile `CMD` (default branch only — workers branch on `$START_COMMAND` and skip). **(9) Mobile + frontend polish:** transactions filter selects now stretch to row width on mobile (`w-full sm:w-auto`) so the chevron overlays the select's right edge and is tappable; category cap raised from 10 to 20 in the FE add-button gate to match the backend's existing 20-per-type limit. Spec/plan files: `/Users/rlabrao/.claude/plans/steady-hugging-curry.md`. Commits: `71867db` … `0ab8c77`. Tests: 31/31 in modified modules pass; 489/497 full backend suite (8 unrelated pre-existing failures in auth/budget_v2/settings_api/whatsapp_pin).

- **Mobile UX overhaul + settings polish + bank-logo registry (2026-04-22)** — Closed the web/mobile gap after a dedicated mobile audit. **Viewport & layout fixes:** `viewport` export in `frontend/app/layout.tsx` now sets `width=device-width`, `initialScale=1`, `viewportFit=cover`, `themeColor` (Next 16 no longer auto-injects these); dashboard main wrapper adds `overflow-x-clip` + `min-w-0` + `pb-[calc(6rem+env(safe-area-inset-bottom,0px))]` so the last row never hides behind the floating BottomNav and iOS home-indicator devices get correct safe-area padding; `html, body { overflow-x: clip }` guard in `globals.css`. **Correctness fixes:** `normalizeTxnAmount` trusts the stored sign (was force-negating positive non-income rows, mis-bucketing refunds); `normalizeBalance(balance, currency, provider)` extracted from `BalanceCard` to `app/lib/currency.ts` so `SummaryBar` now shows the same numbers as the Dashboard for USD/BRL/MXN/PEN/COP accounts; `MarkAsCuotaDialog` currency prop widened from `"CLP"|"USD"` to `string` and rendered via `formatStoredAmount` so cuotas on any currency save correctly. **BottomNav 4+1:** primary tabs (Inicio / Gastos / Plan / Notif.) + "Más" button opens a bottom-sheet menu with Compartido / Suscripciones / Configuración. `NAV_ITEMS` unified in `frontend/app/(dashboard)/components/nav-items.ts`. **Unified page header:** new `PageHeader` component with separate `controls` (left — currency toggle + month) and `actions` (right — icon buttons) props, migrated across Dashboard / Transactions / Budgets / Household / Subscriptions / Notifications. Transactions month moved into the header; Budgets chevron pager and Household `<select>` collapsed into the shared `MonthSelector` (now supports `allowAll` + `count` + `size` + `currency` locale). **Settings polish:** DetectedAccountCard stacks on mobile (name/kind/currency + balance, second row with type pill + Ocultar); Categorías add row stacks Gasto/Ingreso/Agregar on a second row so Agregar stops overflowing; WhatsApp toggle absorbed into the Funciones card alongside "Marcar como cuota"; `NotificationsSection.tsx` deleted. **Marcar como cuota toggle:** hidden behind opt-in `showCuotaButton` flag in Zustand (default off), `FeatureTogglesSection` renders the switch. **Bank-logo registry:** new `frontend/public/bank-logos/` folder with 44 official App Store PNGs covering the top 6-7 banks per LATAM country (CL: Banco de Chile, Falabella, Estado, Santander, BCI, Scotiabank, Itaú, BICE, MACH; CO: Bancolombia, Davivienda, BBVA, Banco de Bogotá, Nequi, DaviPlata, AV Villas; BR: Nubank, Itaú, Bradesco, Banco do Brasil, Caixa, Santander, Inter; US: Venmo, Amex, BofA, Chase, Wells Fargo, Citi, U.S. Bank, Capital One, PayPal, Discover; MX: BBVA, Banorte, Santander, Banamex, HSBC, Azteca; PE: BCP, BBVA, Interbank, Scotiabank, Yape). Central `app/lib/bank-logos.ts` registry keyed by both Luka Connect `bank_code` and Plaid `institution_name`; `findBankLogo` falls back to the first token split on `-/–/—/|/:` so decorated names like `"Venmo - Personal"` still resolve. Reusable `BankLogo` component renders PNG at any size with iOS-style rounded corners (22% of side), lazy-loaded, ~2.4 MB total (settings-only, CDN-cached immutably). Wired into `BankConnectionCard`, `PlaidConnectionCard`, and the connect-bank modal picker with fallback to the colored-initials `BankIcon`. Shared primitives added along the way: `app/lib/locale.ts` (`localeForCurrency` / `resolveAppLocale` with full LATAM map), `app/lib/strings.ts` (`toTitleCase`), `app/lib/months.ts` (`monthKey`/`dateFromMonthKey`/`getLastNMonths`), `app/lib/hooks/useBreakpoint.ts`, `components/EmptyState.tsx`, `components/DirectionIcon.tsx`.

- **Venmo / wallet funding-pair detection (2026-04-22)** — Connected wallets (Venmo, PayPal, CashApp) are now treated as user-owned accounts. A new `detect_wallet_pairs` pass in `backend/modules/reconciliation/wallets.py` pairs the bank-side funding leg (e.g. BofA "Venmo" −$30.90) with the canonical wallet-side expense (e.g. Venmo "Nicolas Celasco" −$30.90). Unlike `detect_transfers`, sign is unconstrained (handles both funding and cash-out) and the window is ±5 days (wallet settlement is slower than bank-to-bank). Gated on at least one leg being on a wallet account and the bank-side merchant containing the short wallet token (`venmo` / `paypal` / `cashapp`), so a BofA "PAYPAL" row without a connected PayPal account stays as a real expense. The bank leg is re-typed to `transfer`; the wallet leg keeps its counterparty name. Wired into `reconciliation_tick` (30-day lookback, runs between transfer and refund passes). Plaid `paypal` subtype now maps to `account_kind='wallet'`. **Frontend:** `PairedTransactionCard` detects wallet pairs (one leg has a wallet-token merchant, the other doesn't) and renders them as the real counterparty expense — title is the person's name (e.g. "Nicolas Celasco"), subtitle is `"<wallet> · pagado con <bank>"`, amount is signed red/green matching a regular expense/income, and the row is still expandable to see both legs. `groupPairs` now collapses single-leg pairs back to a normal `TransactionCard` render so filter-by-bank views don't produce misleading "Transferencia · 1 transacción" cards. One-time backfill: `cd backend && .venv/bin/python -m scripts.backfill_wallet_pairs --email <user>@example.com` (dry-run by default; `--apply` to commit). Diag helper: `scripts.diag_wallet_pairs`. Spec: `docs/superpowers/specs/2026-04-21-venmo-wallet-pairs-design.md`, plan: `docs/superpowers/plans/2026-04-21-venmo-wallet-pairs.md`. 11/11 wallet-pair tests + 33/33 reconciliation suite green.

- **Budget personal view rebuild + Sankey drilldown (2026-04-21)** — Five-part session on the Budget page. **(1) Personal Sankey no longer double-counts:** `_fetch_month_transactions`, `_three_month_category_stats`, and `_daily_burn_14d` now `outerjoin(TransactionSplit)` and filter on `split_type='personal' OR IS NULL` (the NULL fallback includes email-ingested txns on non-joint accounts — those have no splits row at ingestion, and the frontend already treats NULL as personal). **(2) New `Gastos del hogar` Level-2 node:** `caller_ratio × (shared MTD spend + household unpaid shared bills)` routed through `spendable_ceiling` via the existing `personal_allocation` param, with `caller_ratio` resolved from `households.split_ratio` mapped to the i-th joined member (1/N fallback). Personal known-bills reader switched to `get_user_personal_unpaid_known_bills` so shared subs no longer leak into the personal Gastos fijos node. **(3) Clickable Sankey + drilldown endpoint:** new `GET /budgets/v2/{household_id}/drilldown?node_id&view&month&currency&limit` returns top-5 transactions for a clicked node (handles `src_*` / `spent_*` / `spent_other` / `gastos_hogar_personal` / `disponible_hogar` / `meta_ahorro*`; hubs + synthetic + `spent_remaining` + `member_*` return empty with a reason). `BudgetSankey` accepts `onNodeClick` / `activeNodeId`; `BudgetDrilldownCard` replaces the `RunwayCard` below each chart with a list of merchant/date/bank/amount, separate state per view. Small help hint above each chart explains the interaction. **(4) Per-category currency caps (migration 040):** `category_budgets.currency` NOT NULL, unique on `(hh, category, month, currency)`; `_category_caps` filters by view currency; schemas + router + `CategoryCapsEditor` expose a per-row `<select>`. **(5) Config modal currency defaults:** every row now defaults to the user's `preferred_currency` (full LATAM set: CLP/USD/COP/MXN/PEN/BRL) via a shared `currencies.ts` helper instead of hardcoded CLP. PersonalAllocationRow got its own editable currency select (was read-only). Dashboard layout collapsed its nested scroll containers into a single document scroll with a `sticky top-0 h-screen` sidebar; the redundant `overflow-x-auto` wrapper around the Sankey is gone.

- **Transaction consolidation fix (2026-04-20)** — Four-phase overhaul of the pending / reconciliation pipeline. **Migration 039:** added `card_last_four`, `refund_pair_id`, `orphaned_at`, `dismissed_by_user` on `transactions`; migrated status vocabulary `pending|confirmed|settled` → canonical `pending|settled|orphan` with CHECK constraint; added `(household_id, transaction_date DESC)` + partial pending + partial pair-id + GIN trigram on `raw_merchant_name` (pg_trgm) indexes; linearized the duplicate-029 branch by renaming `029_user_currencies.py` → `029b_user_currencies.py`. **Pipeline:** new `reconciliation/refunds.py` + `reconciliation/tick.py` orchestrator running 4 passes per household (rematch aging pendings → `detect_transfers` 7d → `detect_refunds` 90d → 14d aging → orphan). Registered on slow worker every 15 min. `detect_transfers` now guards on `user_id` + `currency`; dedup guards on currency + signed-amount + `bank_account_id` parity + skips merchant filter for transfer emails; `apply_match_and_delete_emails` is user-scoped and propagates transfer type. **Plaid:** `_resolve_cc_counterpart` via last-4 match + bank-name substring; CLP 100× scaling bug in modify branch fixed via `mapper.luka_amount_from_plaid`. **Email ingest:** persists `card_last_four`, eagerly resolves `transfer_to_account_id` against household bank accounts. **API:** `GET /transactions/{id}/match-candidates`, `POST /transactions/{id}/link`, `POST /transactions/{id}/dismiss`, `POST /transactions/bulk-action`. `TransactionResponse` exposes `created_at`, `orphaned_at`, `transfer_pair_id`, `refund_pair_id`, `source_type`. `exclude_from_totals` applied to all aggregation paths so transfers, refund pairs, and orphans don't contribute to spend/income. **Frontend:** unified `formatStoredAmount` / `isZeroDecimalCurrency` / `isNegativeStored` in `app/lib/currency.ts`; new `useMatchCandidates` / `useLinkTransaction` / `useDismissTransaction` / `useBulkAction` hooks; `LinkMatchDialog` manual-match picker; `PairedTransactionCard` groups rows sharing `transfer_pair_id` or `refund_pair_id`; `PendingBlock` now shows all three buckets (`awaiting_reconciliation`, `unmatched_email` now populated, `needs_classification`) with per-row shadcn DropdownMenu (Vincular / Marcar como resuelta / Eliminar), color-coded age badge, bulk-select mode with floating toolbar, skeleton + error states, a11y fixes. Spec: `docs/superpowers/specs/2026-04-20-transaction-consolidation-fix-design.md`. One-time cleanup: `cd backend && python3 -m scripts.cleanup_rafael_pending --dry-run`.

- **Auth modernization (2026-04-20)** — End-to-end pass on authentication + household invite flow driven by a full ultrareview. Closed a critical JWT **algorithm-confusion** vulnerability (HS256 accepted alongside ES256 on the JWKS path — forged tokens could impersonate any user) and removed the HS256 fallback entirely after Supabase Dashboard migration to asymmetric keys. Supabase JWT Signing Keys rotated (ES256, P-256) and legacy HS256 secret revoked. Supabase publishable/secret API keys (`sb_publishable_...`/`sb_secret_...`) live in Railway and Vercel; legacy anon/service_role JWTs disabled. `SUPABASE_JWT_SECRET` env var removed across all surfaces. Microsoft OAuth button hidden behind `NEXT_PUBLIC_ENABLE_MICROSOFT_LOGIN` flag (default off) until Outlook ingest ships end-to-end.

- **Household invite flow rewrite (2026-04-20)** — `POST /invite/{token}` with atomic one-time claim (conditional `UPDATE ... RETURNING`), `SELECT ... FOR UPDATE` row lock on the target household so the 5-member cap check + membership insert is race-free. `secrets.token_urlsafe(32)` tokens, case-insensitive email binding, automatic revocation of prior pending invites when a new one is sent. Owner-customized `split_ratio` (e.g., `[70, 30]`) is preserved on member join — only auto-equal defaults are rebalanced. Orphan data migration: when a user joins a group from an individual household, their `bank_accounts` and `transactions` move with them instead of stranding under the abandoned household_id. Typed `InviteError` with stable codes; frontend invite page branches on code instead of matching Spanish substrings.

- **PWA session durability fix (2026-04-20)** — Root cause of "iOS PWA forgets my session after a while": Supabase cookies were being set without `Max-Age` and wiped whenever the installed PWA fully closed. Fixed via `withDurableCookie` helper (`frontend/app/lib/supabase/cookieOptions.ts`) that applies `Max-Age=1y` in both the SSR server client and the Next.js middleware. Browser client now explicitly sets `flowType: "pkce"`, `persistSession: true`, `autoRefreshToken: true`. Middleware swapped from `getSession()` (unverified) to `getClaims()` (local JWKS verify).

- **Auth + invite perf pass (2026-04-20)** — (1) JWT payload Redis cache keyed by `sha256(token)[:16]` with TTL bounded by token `exp` (~15-20ms per request saved). (2) `get_current_user_attached` dependency: `db.merge(user, load=False)` attaches cached User to the session without SQL, removing the duplicate `SELECT user` every mutation endpoint was doing. (3) `GET /auth/me` now serves from the user cache blob (household_id + contribution fields stored inline) — zero DB queries on hit. Cache key bumped to `user:v2:{email}`; invalidation wired into `accept_invite`, `remove_member`, `patch_contribution_settings`, profile updates. (4) Middleware matcher tightened to skip `_next/data`, `_next/webpack`, the OAuth callback, service-worker / workbox files, manifest, maps, CSS/JS/font extensions — `getClaims()` used to run ~6-10× per page load. (5) `BudgetSankey` dynamic-imported (chart is ~40KB of d3); orphan `PaceChart.tsx` deleted. `@next/bundle-analyzer` wired behind `ANALYZE=true`.

- **OAuth callback parallelize + login UX polish (2026-04-20)** — Callback route runs `/auth/me` in parallel with `store-provider-tokens`; `setup-email-watch` fire-and-forget (backend idempotent on retry), cutting 2-5s of sequential backend calls off the critical path. Redirect-cookie validated as same-origin-relative to close open-redirect phishing. Login page reads `?error=…` into an `aria-live` alert with Spanish copy for `auth_failed`/`no_session`/`no_code`. OAuth buttons disable + `aria-busy` while pending. `document.cookie` write moved out of render body into `useEffect` (React 19 rules). `Secure` flag added on HTTPS.

- **Hot-path DB indexes migration 038 (2026-04-20)** — Three partial btree indexes on `household_members` and `household_invites` covering the auth hot path: `ix_household_members_user_active`, `ix_household_members_household_active`, `ix_household_invites_pending`. Applied directly via asyncpg against production (alembic chain has a pre-existing duplicate-029 conflict — see "Known Issues" below).

- **Budget config modal on /budgets** — A single accordion modal triggered by a gear button next to the currency toggle on `/budgets`, consolidating the 5 budget knobs (savings target, payday, personal allocation, contribution mode, per-category caps) that used to live scattered across `/settings`. The category-caps editor is redesigned: empty-by-default list of active caps + a `+ Agregar tope` picker that surfaces the top-5 spent categories from the current month's `budget-v2` Sankey data. Per-row save mutations with 900ms auto-collapse + "Guardado ✓" chip. Mobile becomes a bottom sheet. Spec: `docs/superpowers/specs/2026-04-15-budget-inline-config-design.md`.

- **Budget v3 Sankey redesign** — 4-level hogar Sankey (per-source income → `Ingresos Hogar` hub → 5 allocation nodes including `Gasto personal` → per-category breakdown) and 3-level personal Sankey with its own `Mis ingresos` hub. Caller-relative privacy: each viewer sees their own income categories broken out at Level 0, other members appear as one aggregated node per member. Privacy invariant enforced by construction in `contribution_service.income_breakdown_for_household_view`. Migrations 036 (`subscription_overrides.split_type`) + 037 (`user_budget_settings.personal_allocation_amount`).

- **Subscription classification toggle** — `Personal`/`Compartido` pill on each detected subscription in `/subscriptions`. Cascades atomically to the last 3 months of `transaction_splits` rows via `reclassify_subscription_split`, upserts the override on `subscription_overrides`, and invalidates the detection cache. Personal subscriptions correctly excluded from the household pot.

## In Progress

- **Template Agent production monitoring** — Template agent deployed (2am daily cron on slow worker). Monitoring first autonomous template generations and shadow validations. First LLM-parsed transactions confirmed live (BofA via gemini-2.5-flash, 0.98 confidence).
- **Plaid production rollout** — Plaid production credentials configured and deployed. BofA connected successfully, 290 transactions synced. Zelle person extraction and CC payment detection working. Pending (processing) transactions now visible in frontend. OAuth redirect URIs configured.

## Pending

### Auth/security follow-ups (from ultrareview — medium severity)

- **Rate-limit `/send-whatsapp-pin` and `/verify-whatsapp-pin`** — no throttle today; a compromised session can burn SMS credits and bypass the 5-strike lockout by re-triggering send (which resets the attempts counter). Cap send to ~3/10min per user, move attempts state to an independent key that survives resends, switch `random.randint` → `secrets.randbelow`.
- **Partner privacy: `get_contribution_summary` leaks real totals** for members on `fixed` / `reimbursement` contribution mode (`backend/modules/households/service.py:186-260`). MEMORY.md explicitly promises "no raw partner rows ever" — the service currently returns per-user totals regardless of `contribution_mode`. Honor mode at query time.
- **Sole-owner-can't-leave** — if you're the only owner of a group with other members, there's no way to leave without deleting your account. Add a "leave household" endpoint that auto-promotes the oldest remaining member before letting you out.
- **`phone_whatsapp` not unique across users** → ambiguous inbound WhatsApp webhook routing when two users verify the same number. Add a DB unique constraint + reject-on-conflict in `verify_whatsapp_pin`.
- **Partial-auth loop edge case** — `exchangeCodeForSession` with a non-null error but no session could leave a partial cookie state that loops. `signOut()` is now called on the `no_session` branch; verify no other branches can reach the partial state.
- **Open invite endpoint rate-limit** — no throttle on `/invite/{token}` today. UUID is unguessable but add a 20/min/IP limit anyway to close the amplification oracle risk.

### Auth/perf follow-ups (deferred from the perf pass)

- **`api.ts` bundle size (~67 KB)** — single-file re-export surface is large; splitting by domain module would let per-page bundles drop unused paths.
- **`settings/page.tsx` ~48 KB incl. `BankAccountsSection.tsx` ~26 KB** — candidate for section-level dynamic import.
- **Next 16 middleware → proxy rename** — deprecation warning surfaces on every build; not functionally urgent. Rename when touching the file next or when bumping to Next 17.

### Household UX follow-ups (from ultrareview — medium)

- **`CompartidoSection` loading + error + empty states** — briefly renders "Individual" on cold load even if the user is in a household; no error fallback if the fetch throws.
- **CompartidoSection Spanish + design polish** — `"Tu"` → `"Tú"`, drop the hardcoded `"Activo"` badge (drive from `member.status`), replace per-row `"Miembro"` label with role or drop entirely. Swap the hand-rolled card styling for shadcn `Card` to match the rest of settings.
- **Structured invite success toast** — `invite/[token]/page.tsx` currently redirects silently on success; a short "¡Te uniste al grupo!" toast closes the UX loop.

### Budget v3 follow-ups (deferred from review notes — non-blocking)

These items were caught by code-quality reviewers during the Plan 1 + Plan 2 sprints and explicitly deferred to keep velocity. None block any current functionality.

- **Migration constraint naming inconsistency** (Plan 1 Task 1 review): `ck_subscription_overrides_split_type` (plural) vs the existing `ck_subscription_override_status` / `_day` (singular) on the same table. Cosmetic; consider a follow-up migration that renames the older two for consistency, or accept the new plural form as canonical.
- **Race condition in `reclassify_subscription_split` cascade** (Plan 1 Task 4 review): the per-tx UPDATE-then-INSERT loop can create duplicate `transaction_splits` rows if two requests race for a tx that has no existing split row. Schema doesn't enforce uniqueness on `transaction_splits.transaction_id`. Recommended fix: add a UNIQUE constraint migration on `transaction_splits.transaction_id` and convert the loop to `INSERT ... ON CONFLICT (transaction_id) DO UPDATE`. Also benefits 4 other insertion sites in the codebase.
- **Test fixture model registration gap** (Plan 1 Task 4 review): `noqa: F401` model imports are sprinkled across `test_subscription_reclassify.py`, `test_user_budget_settings.py`, `main.py`, `alembic/env.py`, and `tests/conftest.py` to register FK targets in SQLAlchemy metadata. The right fix is a central `backend/modules/__init__.py` (or similar) that imports all models once.
- **Test spacing in cascade tests** (Plan 1 Task 4 review): `test_subscription_reclassify.py::test_cascade_updates_last_3_months_only` uses `timedelta(days=31 * months_ago)` instead of `dateutil.relativedelta(months=N)`. Works correctly with an inline comment, but `relativedelta` would be more idiomatic.
- **Helper duplication across test files** (Plan 1 Task 5 review): `_get_seed_user`, `_get_seed_household_id`, `_user_by_email` are now defined in 4+ test files with slight naming variations. A shared `backend/tests/helpers/seed.py` would consolidate them.
- **`get_budget_v2` size growth** (Plan 2 Task 9 review): grew significantly with the inline personal-view bucketing logic and, after the 2026-04-21 drilldown work, `v2_service.py` is now ~1.7k lines; could be extracted to a private helper or split into `v2_service.py` + `v2_sankey.py` + `v2_queries.py` + `v2_drilldown.py`.
- **`update_personal_allocation` and `update_savings_target` parallel implementations** (Plan 2 Task 11 review): could share a single `update_user_budget_settings_field` helper.
- **Hogar Sankey allocation-node `if` blocks repetition** (Plan 2 Task 7 review): the 5 `if X > _ZERO: nodes.append(SankeyNode(...))` stanzas could be DRY'd via a list-of-tuples loop. Cosmetic.
- **Missing `otras_fuentes` synthetic-source unit test** (Plan 2 Task 7 review): `test_budget_v3_sankey.py` doesn't directly test the overspent-month `otras_fuentes` emission path. The logic is correct by inspection and integration-tested via the flow conservation matrix, but a focused unit test would harden coverage.

### Email Pipeline

- **WhatsApp message templates** — Need to create Meta-approved WhatsApp message templates (`verification_code`, `transaction_alert`) for 24-hour window bypass. Currently limited to 24h response window.
- **Income-aware WhatsApp flow** — LLM + split flow not yet income-aware for inflows. WhatsApp alerts fire for income transactions but the conversational flow assumes expenses.

### Outlook Integration

- **Microsoft Azure / Outlook push notifications** — Azure app registration needed. Requires: `Mail.Read` delegated permission, `offline_access` scope for refresh tokens, redirect URI, `MICROSOFT_CLIENT_ID`, `MICROSOFT_CLIENT_SECRET`, `OUTLOOK_CLIENT_STATE`. Code exists (`OutlookProvider`) but credentials not configured. Login button currently hidden via `NEXT_PUBLIC_ENABLE_MICROSOFT_LOGIN=false` until ingest ships.

### Merchant System

- **Merchant training curation** — Use `/train` UI to verify, merge, and fix merchant categories. Initial seed: 161 canonical merchants from 229 raw names. Ongoing quality improvement.

### LATAM Expansion

- **Collect real email samples** — Bank registry has 101 banks seeded but most lack real email samples for template generation. Priority: Chilean banks (Banco Falabella, BCI, Banco Estado, Banco Itau, Scotiabank), then Colombian and Mexican banks.
- **Template Agent tuning** — Adjust `TEMPLATE_AGENT_MIN_EMAILS` (currently 20) and `TEMPLATE_AGENT_RECENCY_DAYS` (14) based on real-world email volume per bank.

### Plaid follow-ups (from 2026-04-27 session)

- **Plaid webhook handler** — `LinkTokenCreateRequest` doesn't yet register a webhook URL with Plaid, and there's no `/webhooks/plaid` endpoint. The 5-step initial-sync retry loop covers most of the "first sync runs before Plaid finishes ingesting" cases, but a proper handler for `HISTORICAL_UPDATE` / `DEFAULT_UPDATE` / `SYNC_UPDATES_AVAILABLE` would (a) eliminate the wait entirely on slow institutions and (b) let Luka catch `ITEM_LOGIN_REQUIRED` re-auth requests instead of letting items go silently stale.
- **Camila's stranded "Ropa" rows** — 32 transactions in household `8737617e-37a2-40bb-9dcc-f1c592db7b49` still carry `category='Ropa'` after a buggy delete that fired before the 2026-04-27 fixes. Pending Camila's UI redo: re-add "Ropa" → delete via the now-correct modal → pick "Sin categoría" (or another category). One-shot SQL alternative if the UI flow doesn't happen: `UPDATE transactions SET category = NULL WHERE household_id = '8737617e-37a2-40bb-9dcc-f1c592db7b49' AND category = 'Ropa';`
- **Investigate double-PATCH on category change** — During the 2026-04-27 debug session a single mobile category tap fired two PATCH requests on the same transaction 21 seconds apart (logged `eb579b6c` SEPTA row, both 200 OK). Could be: an iOS Safari double-tap artefact, a stale React effect, or user re-tapping after they thought it didn't save. Worth instrumenting (request-id correlation via PostHog or a backend `seen_request_ids` LRU) to disambiguate, and widening the category-badge touch target on mobile (currently ~80 px wide) so easy mistaps stop being a category of "is this a UI bug or a finger problem" support questions.

### Multi-Card Reconciliation

- **Retroactive CC counterpart reclassification** — `_resolve_cc_counterpart` (Task 2.5) only runs at ingest time. Rows ingested before the fix may still be typed `expense`/`income` when they should be `transfer`. A one-time backfill script that replays counterpart resolution over historical transactions would close this gap.

### Transaction consolidation follow-ups (non-blocking)

- **Auto-delete orphans after 180d** — not in scope for the 2026-04-20 fix. Orphans currently live forever in the `unmatched_email` bucket; a scheduled prune would keep the list tidy.
- **Toast helper (sonner) for link/dismiss feedback** — current flows use ad-hoc alerts / silent redirects. A shared `toast` utility would unify success/error copy.
- **Migrate inline category/split pill editors to shadcn `Select`** — the inline pills on transaction rows are hand-rolled dropdowns; porting to shadcn `Select` would give proper keyboard navigation and a11y out of the box.

### Phase 2 Forecast Engine (carried from budget-v2 sprint)

- **Phase 2 forecast engine** — Swap `forecast.py` heuristic internals for a Bayesian engine (same function signatures; contract is stable per the 2026-04-14 spec §6.2). Adds uncertainty bands to the risk alert band and better overshoot probabilities for low-history categories.
- **Redis caching for risk categories** — The 2026-04-14 spec §6.3 mentions `budget:risk:{user_id}:{YYYY-MM}` cache with month-long TTL. Deferred in sprint; `v2_service.py` has a `TODO(Phase 2)` marker at the risk-computation block.

## Known Issues

- **No browser-based UAT for authenticated flows** — Google OAuth's automation-detection blocks chrome-devtools-mcp + browser-use from signing into Luka. Auth changes are verified via TypeScript build + backend curl tests (401 paths, HS256 alg-confusion rejection) + user's own manual smoke. Pre-release tagging should still include a manual click-through by the maintainer.
- **WhatsApp Business API approval** — Currently using test/dev mode. For multi-user production use, Meta's official WhatsApp Business API approval process is needed.
- **No frontend test infrastructure** — No Jest, Vitest, or Playwright configured. Backend has 56 test files (~401 tests) but frontend has zero tests.
- **No CI/CD pipeline** — No GitHub Actions workflows. Backend tests are run manually (`pytest`). Deployment is manual via Railway/Vercel.
- **Pre-existing `test_sankey_flow_conservation_fixed_outflow_exceeds_income` failure** (`backend/tests/test_budget_v2_endpoint.py`) — inserts a transaction with `status='categorized'`, but migration 039 tightened `ck_transactions_status` to `('pending','settled','orphan')`. Fails in isolation; unrelated to any of the 2026-04-21 budget work. Fix by updating the seed to a valid status (or loosening the check constraint if 'categorized' is needed).

## Infrastructure TODOs

- **Luka Connect env vars** — Verify `LUKA_CONNECT_URL`, `LUKA_CONNECT_API_KEY`, `CONNECT_ENCRYPTION_KEY`, `BACKEND_PUBLIC_URL` are set on production Railway.
- **Supabase OAuth providers** — Google OAuth confirmed enabled. Azure provider should remain disabled in the Supabase Dashboard until the Outlook ingest flow is production-ready (login button is already hidden by env flag, but the provider can still be triggered by direct URL if enabled).

## Future Ideas

- Category budget alerts via WhatsApp (notify when spending approaches category limit)
- Template Agent dashboard — admin UI to view template status, accuracy, shadow validation results per bank
- Bank registry admin API — CRUD endpoints for managing bank_registry entries
- Per-transaction `is_shared` override — a user might want to override a one-off transaction without touching the subscription rule (the v3 sprint considered this and explicitly deferred it; out of scope for v3)
- Cross-currency caps aggregation in the Config modal — today the summary shows "N topes activos · en M monedas" without a total. A per-currency rollup would help users spot which currency dominates their caps.
